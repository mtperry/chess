mod board;
mod error;

use crate::board::*;

fn main() {
    
    //let mut magic_file = std::fs::File::create("magic_tables.rs").expect("Failed to create magic_tables.rs");
    //write_magic(&mut magic_file).expect("Failed to write magic_tables.rs");
}

